# 工具详情页和关联资讯功能

## 功能概述

实现了工具详情页面功能，每个工具都有独立的详情页，展示工具信息和关联的资讯文章。

## 核心特性

### 1. 工具标识符（Identifier）

每个工具都有一个隐藏的 `identifier` 字段，用于和资讯的 `tool_tags` 关联：

- **字段名**：`identifier`
- **生成规则**：基于工具名称，转换为小写，保留字母、数字、连字符和下划线
- **用途**：在归档文章时，可以将工具的 `identifier` 添加到文章的 `tool_tags` 中，实现精确关联

**示例**：
```json
{
  "name": "keep",
  "identifier": "keep",
  ...
}
```

### 2. 工具详情页面

**路由**：`/tool/{tool_id}`

**功能**：
- 显示工具完整信息（名称、图标、描述、分类、标签、访问量）
- 显示"访问工具"按钮，点击后跳转到工具官网
- 显示"相关资讯"部分，展示与该工具关联的文章
- 支持刷新相关资讯
- 支持返回上一页

### 3. 关联资讯匹配机制

系统通过以下方式匹配相关文章：

1. **优先使用 identifier**：如果文章的 `tool_tags` 中包含工具的 `identifier`，则匹配
2. **备用使用工具名称**：如果没有 identifier 或 identifier 未匹配，则使用工具名称进行模糊匹配

**匹配逻辑**：
```python
# 优先使用 identifier（精确匹配）
if tool_identifier in article.tool_tags:
    matched = True

# 如果没有 identifier 或未匹配，使用工具名称（模糊匹配）
if not matched and tool_name:
    if tool_name in article.tool_tags or article.tags:
        matched = True
```

### 4. API 接口

#### 获取工具详情
```
GET /api/tools/{tool_id}
```

**响应**：
```json
{
  "id": 1,
  "name": "keep",
  "url": "https://github.com/keephq/keep",
  "description": "...",
  "category": "devops",
  "tags": [],
  "icon": "🔧",
  "view_count": 10,
  "created_at": "2024-12-03T00:00:00Z",
  "is_featured": false,
  "identifier": "keep",
  "related_articles": [...],
  "related_articles_count": 5
}
```

## 使用方法

### 1. 为工具添加 identifier

**手动添加**：为现有工具添加 identifier：
```bash
python scripts/add_tool_identifiers.py
```

**注意**：identifier 会在添加工具数据时自动生成，无需手动设置。

### 2. 关联文章和工具

在管理员面板归档文章时，可以在"工具标签"字段中输入工具的 `identifier`，多个标签用逗号分隔。

**示例**：
- 工具：`keep` (identifier: `keep`)
- 归档文章时，工具标签填写：`keep, devops`

### 3. 访问工具详情页

- **从工具列表**：点击工具卡片，跳转到 `/tool/{tool_id}`
- **直接访问**：在浏览器中输入 `/tool/{tool_id}`

## 前端实现

### 工具详情页布局

```
┌─────────────────────────────────────┐
│  ← 返回分类                          │
│                                     │
│  [图标] 工具名称                      │
│        分类 • 🔥 X 次访问            │
│        [访问工具]                    │
│                                     │
│  工具描述                            │
│  ...                                │
│                                     │
│  标签                                │
│  [tag1] [tag2]                      │
│                                     │
│  ───────────────────────────────    │
│                                     │
│  📚 相关资讯          [刷新] [查看更多]│
│                                     │
│  [文章卡片1]                         │
│  [文章卡片2]                         │
│  ...                                │
└─────────────────────────────────────┘
```

### 相关资讯显示

- 显示文章标题、来源、发布时间、摘要
- 显示文章分类标签（编程资讯/AI资讯）
- 显示工具标签（如果有）
- 支持点击文章跳转到原文
- 支持刷新获取最新关联文章

## 数据模型

### 工具数据模型
```json
{
  "id": 1,
  "name": "工具名称",
  "url": "https://example.com/tool",
  "description": "工具描述",
  "category": "devops",
  "tags": ["标签1", "标签2"],
  "icon": "🔧",
  "view_count": 0,
  "created_at": "2024-12-03T00:00:00Z",
  "is_featured": false,
  "identifier": "tool-name"  // 新增字段
}
```

### 文章数据模型（关联部分）
```json
{
  "id": "article_123",
  "title": "文章标题",
  "url": "https://example.com/article",
  "tool_tags": ["keep", "devops"],  // 工具标识符列表
  "tags": ["标签1", "标签2"],
  ...
}
```

## 注意事项

1. **identifier 唯一性**：每个工具的 identifier 应该是唯一的，建议使用工具名称的小写版本
2. **中文工具名称**：对于中文工具名称，identifier 可能包含中文字符，建议在归档文章时使用英文 identifier
3. **匹配优先级**：identifier 匹配优先于工具名称匹配
4. **工具标签格式**：归档文章时，工具标签应该使用工具的 identifier，而不是工具名称

## 相关文件

- `app/services/data_loader.py` - 数据加载和关联查询逻辑
- `app/routes/api.py` - 工具详情 API 接口
- `app/main.py` - 前端工具详情页实现
- `scripts/add_tool_identifiers.py` - 为工具添加 identifier 的脚本

